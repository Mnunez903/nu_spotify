require 'spec_helper'

describe NuSpotify do
  it 'has a version number' do
    expect(NuSpotify::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
